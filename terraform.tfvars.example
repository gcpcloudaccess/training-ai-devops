# Copy to terraform.tfvars and fill in your values.

project_id    = "auto-app-healer"
region        = "us-central1"
repository_id = "my-repo"
format        = "DOCKER"
description   = "Artifact Registry repository managed by Terraform"

labels = {
  team = "platform"
}
